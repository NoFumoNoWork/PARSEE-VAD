# Manifests

Place public-safe decision-manifest CSVs here. The expected filenames and schema are
documented in [`../../docs/DATASETS.md`](../../docs/DATASETS.md).

Do not publish internal manifests containing private absolute paths, hostnames, or
usernames.
